{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preflight": "tsx scripts/preflight.ts",
    "test": "pnpm preflight && playwright test",
    "test:portal": "pnpm preflight && playwright test tests/portal",
    "test:backoffice": "pnpm preflight && playwright test tests/backoffice",
    "install-browsers": "playwright install --with-deps chromium webkit",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.2"
  }
}
